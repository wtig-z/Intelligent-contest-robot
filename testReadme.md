# 测试与说明（业务外）

- **业务内自动化单测**：若后续补充 `pytest` 等，建议放在 `tests/` 目录，与 `backend/`、`frontend/` 解耦说明见 `docs/STRUCTURE.md`。
- **论文对比实验、聚合指标与制表**：见独立目录 **`paper_evaluation/`**（含 `评测说明.md`、制表脚本与示例 JSON），**不依赖**本仓库 `backend` / `frontend` 的 import。

仓库根目录 **`README.md`** 为产品说明，本文档不替代之。
